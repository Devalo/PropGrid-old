<%= form_with(model: foobar, local: true) do |form| %>
  <% if foobar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(foobar.errors.count, "error") %> prohibited this foobar from being saved:</h2>

      <ul>
        <% foobar.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :doc %>
    <%= form.file_field :doc, direct_upload: true %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
